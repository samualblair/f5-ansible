# These requirements are for dedicated python DO generate script - not necessarily ansible usage
# Last tested with Jinja 3.1.6 and MarkupSafe 3.0.3
# Jinja2==3.1.6
# MarkupSafe==3.0.3

# Should generally be safe with newer versions
Jinja2 >= 3.1.6
MarkupSafe >= 3.0.3
