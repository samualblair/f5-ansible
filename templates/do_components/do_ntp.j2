"NTP": {
            "class": "NTP",
            "servers": [
                "{{ item.unit.ntp_server_1 }}",
                "{{ item.unit.ntp_server_2 }}",
                "{{ item.unit.ntp_server_3 }}"
            ],
            "timezone": "{{ item.unit.ntp_timezone }}"
        }